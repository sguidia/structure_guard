// Generated by CoffeeScript 1.7.1
var ready;

ready = function() {
  var init;
  Element.prototype.hasClassName = function(a) {
    return new RegExp("(?:^|\\s+)" + a + "(?:\\s+|$)").test(this.className);
  };
  Element.prototype.addClassName = function(a) {
    if (!this.hasClassName(a)) {
      return this.className = [this.className, a].join(" ");
    }
  };
  Element.prototype.removeClassName = function(b) {
    var a;
    if (this.hasClassName(b)) {
      a = this.className;
      return this.className = a.replace(new RegExp("(?:^|\\s+)" + b + "(?:\\s+|$)", "g"), " ");
    }
  };
  init = function() {
    var box, i, len, onButtonClick, panelClassName, showPanelButtons, _results;
    box = document.getElementById("structure_model");
    showPanelButtons = document.querySelectorAll("#show-buttons button");
    panelClassName = "show-front";
    onButtonClick = function(event) {
      box.removeClassName(panelClassName);
      panelClassName = event.target.title;
      return box.addClassName(panelClassName);
    };
    i = 0;
    len = showPanelButtons.length;
    _results = [];
    while (i < len) {
      showPanelButtons[i].addEventListener("click", onButtonClick, false);
      _results.push(i++);
    }
    return _results;
  };
  return init();
};

$(document).ready(ready);

$(document).on('page:load', ready);

//# sourceMappingURL=structure_models.js.map
