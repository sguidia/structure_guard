// Generated by CoffeeScript 1.7.1
var ready;

ready = function() {
  var required_form;
  alert('foo');
  required_form = $(".required_form");
  if (required_form) {
    return required_form.each(function() {
      var required_fields, required_submit;
      required_fields = $(this).find(".required");
      required_submit = $(this).find(".required_submit");
      return required_fields.change(function() {
        if (required_fields.is(":empty")) {
          return required_submit.addClass("disabled");
        } else {
          return required_submit.removeClass("disabled");
        }
      });
    });
  }
};

$(document).ready(ready);

$(document).on('page:load', ready);

//# sourceMappingURL=required_forms.js.map
